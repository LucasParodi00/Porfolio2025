import { NavbarContainer } from "./NavbarContent"; // Asumiendo que guardaste el container aquí
import { NavbarLinks } from "./NavLinks";

export const Navbar = () => {
  return (
    <NavbarContainer>
      <NavbarLinks />
    </NavbarContainer>
  );
};
